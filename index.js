const numbers = [1, 2, 3];

const joined = numbers.join(',');

console.log(typeof numbers);
console.log( joined.split(','));

const messege = 'This is my first message';
const parts = messege.split('-');

console.log(parts);
